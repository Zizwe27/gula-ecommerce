# Dependencies
node_modules/

# Stray lockfile at repo root (Expo app lives in mobile/)
/package-lock.json

# Environment (secrets) — do not ignore .env.example
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS / IDE
.DS_Store
.idea/

# Claude Code (local permissions)
.claude/settings.local.json

# Logs
*.log
npm-debug.*

# Supabase local (if used)
supabase/.branches
supabase/.temp
